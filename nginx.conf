server {
    listen 80;
    server_name localhost;

    location / {
        # Forward all requests to the FastAPI container (named "fastapi" in the Compose file)
        proxy_pass http://fastapi:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

